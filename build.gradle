plugins {
    id 'application'
}

group = 'com.webapp'
version = '1.0.0'

repositories {
    mavenCentral()
}

java {
    toolchain {
        languageVersion = JavaLanguageVersion.of(21)
    }
}

application {
    mainClass = 'com.webapp.App'
}

tasks.withType(JavaCompile).configureEach {
    options.encoding = 'UTF-8'
}
